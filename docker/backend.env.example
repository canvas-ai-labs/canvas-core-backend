# Demo mode: 1 = use mock data, 0 = require auth + Canvas API
DEMO_MODE=1
ENV=dev

# Canvas API Configuration (required if DEMO_MODE=0)
CANVAS_API_URL=https://canvas.your-school.edu
CANVAS_API_KEY=your-canvas-api-token-here

# Security Keys - CHANGE THESE IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-change-me-32-chars
SECRET_KEY=your-super-secret-fernet-key-change-me-32chars

# CORS Origins - JSON array format for pydantic-settings
ALLOWED_ORIGINS=["http://localhost:3001","http://127.0.0.1:3001"]